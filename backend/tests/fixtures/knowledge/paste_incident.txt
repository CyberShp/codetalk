iSCSI CMD resource exceeds 8K and IO drops to zero.
After reducing the resource count below 8K, IO does not recover.
The advertised available CMD count keeps shrinking because a CmdSN stops advancing.
