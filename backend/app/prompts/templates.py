"""Chinese prompt templates for CodeTalk report generation.

Each template is a format-string that accepts structured context (code graph data,
wiki content, module info) and instructs the LLM to generate a specific report section.
"""

_FORMATTING_RULES = """\
## 排版规范（违反以下任何一条将被视为不合格输出）
- 所有结构化数据 **必须** 使用 Markdown 表格，禁止用纯文字列表替代。表格须有表头行和分隔行
- 每个主要流程/模块 **必须** 附上 Mermaid 图（用 ```mermaid 代码块包裹，类型为 sequenceDiagram 或 graph TD）
- 标题层级严格遵循：## 一级章节 / ### 二级章节 / #### 三级章节，禁止跳级
- 代码块 **必须** 使用 ```language 标注语言（如 ```python、```typescript、```go）
- 每个章节开头 **必须** 有一句话摘要，然后再展开详细内容
- 不设字数上限，分析深度优先于篇幅控制
"""

# ---------------------------------------------------------------------------
# Per-module summary (MapReduce map phase)
# ---------------------------------------------------------------------------

MODULE_SUMMARY_PROMPT = """\
你是一名资深代码分析专家。请根据以下模块信息，生成一份详尽的模块摘要。

## 模块名称
{module_name}

## 模块文件列表
{file_list}

## 代码结构（定义关系）
{structural_info}

## 模块内调用关系
{call_relations}

## 模块 Wiki 文档
{wiki_content}

{source_code_section}

## 分析要求
- **严格基于上方提供的数据进行分析**，不要臆测未提供的信息
- 如果提供了源代码，请直接从代码中提取函数签名、调用链和数据结构
- 如果某项数据缺失（标注"无"），在对应输出章节中注明"数据不足，无法分析"而非编造

## 输出要求
请用中文输出，包含以下部分：
1. **模块职责**（一句话概括核心定位）
2. **核心组件**（列出 3-5 个关键文件/类/函数及其作用）
3. **对外接口**（该模块暴露给其他模块的 API/函数/类，含参数说明）
4. **内部依赖**（模块内部的关键调用链路，说明数据如何流转）
5. **技术要点**（使用的设计模式、框架特性、并发模型、特殊算法等）
6. **调用时序**（描述模块在系统生命周期中的调用时机：初始化阶段 / 运行态 / 清理态）

各条目深度优先于篇幅控制：职责/接口类条目可简练，技术要点/调用时序类条目需充分展开。

## 输出格式
- 核心组件 **必须** 用表格展示（列：组件名 | 职责 | 关键类/函数）
- 对外接口 **必须** 用表格展示（列：接口名 | 参数 | 返回值 | 说明）
- 调用时序 **必须** 用 Mermaid sequenceDiagram 展示
"""

# ---------------------------------------------------------------------------
# Report 01: 项目与模块地图
# ---------------------------------------------------------------------------

MODULE_MAP_PROMPT = """\
你是一名资深软件架构师。请根据以下项目信息，生成《项目与模块地图》报告。

{_formatting_rules}

## 项目概览
{{project_overview}}

## 模块列表与摘要
{{module_summaries}}

## 模块间依赖关系
{{inter_module_deps}}

## 输出要求

### 1. 项目简介
一段话概括项目定位、技术栈和整体架构风格（含并发模型、部署形态）。

### 2. 模块地图（表格）
用表格列出所有模块：

| 模块名称 | 核心职责 | 入口文件 | 估计代码行数 | 复杂度评级 | 依赖的其他模块 |
|---------|---------|---------|------------|-----------|--------------|

复杂度评级：低（内聚、依赖少）/ 中（中等耦合）/ 高（复杂逻辑或重度依赖）

### 3. 架构图（Mermaid）
用 `graph TD` 绘制模块依赖关系图，体现分层结构（如：接入层 → 业务层 → 数据层）：

```mermaid
graph TD
    A[模块A] --> B[模块B]
    ...
```

### 4. 模块分层分析
描述各模块在架构中的层级归属，以及跨层调用的情况。

### 5. 技术栈清单（表格）

| 类别 | 技术/框架 | 版本 | 用途 |
|------|---------|------|------|

### 6. 架构风险点
列出潜在的单点依赖、循环依赖或高耦合模块，及其影响范围。
""".replace("{_formatting_rules}", _FORMATTING_RULES).replace("{{", "{").replace("}}", "}")

# ---------------------------------------------------------------------------
# Report 02: 关键业务流程分析
# ---------------------------------------------------------------------------

BUSINESS_FLOW_PROMPT = """\
你是一名业务分析专家。请根据以下信息，生成《关键业务流程分析》报告。

{_formatting_rules}

## 模块摘要
{{module_summaries}}

## 进程/流程数据
{{process_data}}

## 跨模块调用关系
{{cross_module_calls}}

## 输出要求

### 1. 核心业务流程清单
列出系统中 3-5 个最重要的业务流程（用一行说明触发条件和终态）。

### 2. 流程详细分析

对每个流程，按以下三层结构展开分析（每个流程不少于 400 字）：

#### 2.x [流程名称]

**层一：用户场景**
- 触发方式（用户操作 / 定时任务 / 事件驱动 / Webhook）
- 用户期望的输入和输出
- 典型使用场景和边界场景

**层二：系统行为**
- 涉及的模块和组件（按调用顺序列出）
- 状态机转换（如有明显状态流转）
- 并发模型（同步 / 异步 / 队列 / 并行分支）
- 时序图（必须附上 Mermaid sequenceDiagram）：

```mermaid
sequenceDiagram
    participant U as 用户/调用方
    participant A as 模块A
    participant B as 模块B
    U->>A: 请求
    A->>B: 调用
    B-->>A: 返回
    A-->>U: 响应
```

**层三：代码路径**
- 入口函数/方法（文件名 + 函数名）
- 关键调用链（`module.function() → module2.function2() → ...`）
- 核心数据结构在各层的形态变化
- 与外部系统（数据库、第三方 API、消息队列）的交互点

**异常分支分析**（表格）：

| 异常类型 | 触发条件 | 影响范围 | 恢复策略 |
|---------|---------|---------|---------|

---

### 3. 流程间关系
说明各流程之间的依赖和触发关系（如 A 流程完成后触发 B 流程）。

### 4. 生命周期时序分析
描述系统在 **初始化阶段 → 运行态 → 清理/关闭阶段** 的流程执行顺序和资源管理方式。

### 5. 风险点识别（表格）

| 风险项 | 所在流程 | 类型（性能/可靠性/安全） | 严重程度 | 建议措施 |
|-------|---------|----------------------|---------|---------|
""".replace("{_formatting_rules}", _FORMATTING_RULES).replace("{{", "{").replace("}}", "}")

# ---------------------------------------------------------------------------
# Report 03: 源码定向阅读记录
# ---------------------------------------------------------------------------

SOURCE_READING_PROMPT = """\
你是一名代码审阅专家。请根据以下信息，生成《源码定向阅读记录》。

{_formatting_rules}

## 模块摘要
{{module_summaries}}

## 重点文件与函数
{{key_files}}

## 调用图数据
{{call_graph}}

## 输出要求

### 1. 阅读路线图
建议的源码阅读顺序（从系统入口 → 核心业务逻辑 → 数据层 → 辅助工具），并说明每一步的阅读目标。

### 2. 核心文件解读

对每个关键文件，输出：

#### [文件路径]

**职责摘要**：一句话说明文件的核心定位。

**关键函数/类（表格）**：

| 名称 | 类型 | 参数签名 | 返回值 | 核心逻辑描述 |
|-----|------|---------|-------|------------|

**调用关系图（Mermaid）**：

```mermaid
graph TD
    当前文件 --> 依赖文件A
    当前文件 --> 依赖文件B
```

**值得关注的实现细节**：
- 非显而易见的算法或数据结构选择
- 并发/异步处理方式
- 错误处理策略
- 潜在的性能或安全隐患

---

### 3. 设计模式识别（表格）

| 设计模式 | 所在文件/类 | 作用描述 |
|---------|-----------|---------|

### 4. 代码质量观察（表格）

| 维度 | 评级（优/良/待改进） | 观察与建议 |
|-----|-------------------|---------|
| 命名规范 | | |
| 函数粒度 | | |
| 错误处理 | | |
| 注释质量 | | |
| 测试覆盖 | | |
| 并发安全 | | |

### 5. 推荐精读入口
列出 3-5 个"读懂这个文件就能理解整个系统"的核心文件，并说明理由。
""".replace("{_formatting_rules}", _FORMATTING_RULES).replace("{{", "{").replace("}}", "}")

# ---------------------------------------------------------------------------
# Report 04: 测试设计输入
# ---------------------------------------------------------------------------

TEST_DESIGN_PROMPT = """\
你是一名测试设计专家。请根据以下信息，生成《测试设计输入》文档。

{_formatting_rules}

## 模块摘要
{{module_summaries}}

## 业务流程
{{business_flows}}

## 接口列表
{{api_interfaces}}

## 输出要求

### 1. 测试范围（表格）

| 模块/功能 | 测试类型 | 优先级 | 说明 |
|---------|---------|-------|-----|

### 2. 测试策略（分层）

**单元测试重点**：列出需要优先覆盖的函数/类，说明理由（纯函数、边界逻辑、核心算法等）。

**集成测试场景**：列出模块间交互需要验证的场景，重点关注数据库操作、外部 API 调用、消息队列等集成点。

**端到端测试用例**：列出需要贯穿测试的完整业务流程。

**测试策略图（Mermaid）**：

```mermaid
graph TD
    E2E[端到端测试] --> IT[集成测试]
    IT --> UT[单元测试]
    UT --> 核心函数A
    UT --> 核心函数B
```

### 3. 关键测试用例（表格）

对每个模块/接口，列出核心测试点：

| 模块/接口 | 测试场景 | 输入 | 预期输出 | 测试类型 |
|---------|---------|-----|---------|---------|

### 4. 边界条件与异常场景（表格）

| 场景类型 | 具体描述 | 涉及模块 | 预期行为 | 测试难度 |
|---------|---------|---------|---------|---------|
| 空输入 | | | | |
| 超大输入 | | | | |
| 并发冲突 | | | | |
| 网络超时 | | | | |
| 数据库不可用 | | | | |

### 5. 测试数据建议
列出测试所需的数据类型、典型样例和 mock 策略。

### 6. 测试覆盖率目标

| 层级 | 目标覆盖率 | 重点覆盖范围 |
|------|-----------|------------|
| 单元测试 | ≥80% | |
| 集成测试 | ≥60% | |
| E2E测试 | 核心流程100% | |
""".replace("{_formatting_rules}", _FORMATTING_RULES).replace("{{", "{").replace("}}", "}")

# ---------------------------------------------------------------------------
# Report 05: 需求与设计理解（可选）
# ---------------------------------------------------------------------------

REQUIREMENTS_PROMPT = """\
你是一名需求分析专家。请根据以下信息，生成《需求与设计理解》文档。

## 需求文档内容
{requirements_doc}

## 设计文档内容
{design_doc}

## 代码模块摘要
{module_summaries}

## 输出要求
请用中文输出，包含以下章节：
1. **需求要点提炼**：从需求文档中提取 5-10 个核心功能需求
2. **设计决策分析**：从设计文档中提取关键的架构决策和技术选型理由
3. **需求-代码映射**：将每个核心需求映射到对应的代码模块/文件
4. **覆盖度评估**：哪些需求已经在代码中实现，哪些可能缺失
5. **疑问清单**：需求文档与代码实现之间的不一致或模糊之处

## 输出格式
- 需求要点 **必须** 用表格展示（列：编号 | 需求名称 | 优先级 | 描述）
- 需求-代码映射 **必须** 用表格展示（列：需求编号 | 需求描述 | 对应模块 | 对应文件 | 覆盖状态）
- 风险项 **必须** 用表格展示（列：风险项 | 影响范围 | 严重程度 | 建议措施）
"""

# ---------------------------------------------------------------------------
# Report 06: 需求-设计-代码追踪（可选）
# ---------------------------------------------------------------------------

TRACEABILITY_PROMPT = """\
你是一名质量保障专家。请根据以下信息，生成《需求-设计-代码追踪矩阵》。

## 需求条目
{requirements_items}

## 设计模块
{design_modules}

## 代码模块摘要
{module_summaries}

## 调用关系
{call_relations}

## 输出要求
请用中文输出，以表格形式呈现追踪矩阵：

| 需求编号 | 需求描述 | 设计模块 | 实现文件 | 测试文件 | 覆盖状态 |
|---------|---------|---------|---------|---------|---------|

追踪矩阵之后，请补充：
1. **覆盖率统计**：已实现/部分实现/未实现的需求占比（**必须** 用表格展示）
2. **风险项**：未覆盖或部分覆盖的需求，及其影响评估（**必须** 用表格展示：风险项 | 未覆盖需求 | 影响评估 | 建议措施）
3. **建议**：提高追踪覆盖率的具体行动项

## 输出格式要求
- 追踪矩阵**必须**以完整的 Markdown 表格呈现，每一行对应一个需求条目
- 覆盖率统计**必须**用表格汇总（列：状态 | 数量 | 占比）
- 风险项**必须**使用表格（列：需求编号 | 风险类型 | 严重程度 | 建议行动）
"""
