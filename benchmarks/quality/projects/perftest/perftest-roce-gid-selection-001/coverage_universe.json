{
  "case_id": "perftest-roce-gid-selection-001",
  "items": [
    {"item_id": "perftest-gid:entry", "dimension": "entrypoints", "critical": true, "applicability": "required", "statement": "write_bw enters GID handling through set_up_connection, which queries an explicit user index directly or invokes automatic selection when gid_index is set but use_gid_user is false.", "evidence_refs": ["source://src/write_bw.c#L187-L191", "source://src/perftest_communication.c#L892-L928"]},
    {"item_id": "perftest-gid:flow", "dimension": "flows", "critical": true, "applicability": "required", "statement": "Automatic setup scans the port GID table, scores each valid entry, stores the first strictly highest-scoring index, and queries that selected GID for the local destination.", "evidence_refs": ["source://src/perftest_communication.c#L663-L723", "source://src/perftest_communication.c#L914-L929"]},
    {"item_id": "perftest-gid:branches", "dimension": "branches", "critical": true, "applicability": "required", "statement": "A user-supplied index is queried directly, while automatic selection skips failed or zero GIDs, returns -1 if none are valid, and otherwise queries the chosen index.", "evidence_refs": ["source://src/perftest_communication.c#L671-L700", "source://src/perftest_communication.c#L918-L928"]},
    {"item_id": "perftest-gid:states", "dimension": "states", "critical": true, "applicability": "required", "statement": "Parameters start with gid_index=-1 and use_gid_user=0; Ethernet detection changes an unset index to 0 to enable GID handling, and automatic setup replaces it with the selected table index.", "evidence_refs": ["source://src/perftest_parameters.c#L1007-L1013", "source://src/perftest_parameters.c#L4483-L4493", "source://src/perftest_communication.c#L914-L928"]},
    {"item_id": "perftest-gid:resources", "dimension": "resources", "critical": true, "applicability": "required", "statement": "write_bw opens the verbs device context, allocates local and remote destination arrays and test resources before setup, and routes setup failure through context destruction and memory release.", "evidence_refs": ["source://src/write_bw.c#L80-L117", "source://src/write_bw.c#L143-L178", "source://src/write_bw.c#L611-L639"]},
    {"item_id": "perftest-gid:boundaries", "dimension": "boundaries", "critical": true, "applicability": "required", "statement": "Automatic selection examines indices from zero through gid_tbl_len minus one and keeps the earlier index on equal scores because replacement requires a strictly greater score; explicit -x input is range-checked as uint8_t.", "evidence_refs": ["source://src/perftest_communication.c#L644-L723", "source://src/perftest_parameters.c#L3345-L3347"]},
    {"item_id": "perftest-gid:concurrency", "dimension": "concurrency", "critical": false, "applicability": "required", "statement": "In dual-port mode, setup repeats explicit or automatic GID selection for the second port, and QPs are partitioned so the latter half uses that port's selected GID index.", "evidence_refs": ["source://src/perftest_communication.c#L931-L945", "source://src/perftest_resources.c#L3462-L3490"]},
    {"item_id": "perftest-gid:errors", "dimension": "errors", "critical": true, "applicability": "required", "statement": "A failed or all-zero table entry is ineligible, and setup returns -1 when no valid index is selected or when the selected GID query fails on either configured port.", "evidence_refs": ["source://src/perftest_communication.c#L671-L675", "source://src/perftest_communication.c#L914-L945"]},
    {"item_id": "perftest-gid:protocol-roce-gid-selection", "dimension": "protocol", "critical": true, "applicability": "required", "statement": "For RoCE source-GID selection, each candidate is evaluated by IP-family match and link-local scope, with its queried GID type adding a lower-weight version preference in which RoCE v2 outranks v1.", "description": "Automatic GID selection scores supported RoCE versions with v2 preferred over v1; setup queries a user-selected GID directly when use_gid_user is set, otherwise it selects the best scored GID index and fails when no valid index or GID can be queried.", "evidence_refs": ["source://src/perftest_communication.c#L618-L640", "source://src/perftest_communication.c#L652-L700", "source://src/perftest_communication.c#L915-L928"]}
  ]
}
