{
  "case_id": "phosphor-state-hypervisor-boot-progress-001",
  "items": [
    {"item_id": "state-entry-properties-changed", "statement": "Hypervisor construction emits the inherited D-Bus object-added signal and initializes the bootProgressChangeSignal member.", "dimension": "entrypoints", "critical": true, "applicability": "required", "evidence_refs": ["source://hypervisor_state_manager.hpp#L42-L46"]},
    {"item_id": "state-flow-signal-to-state-update", "statement": "bootProgressChangeEvent parses the changed-property map, looks up BootProgress, extracts its string value, and passes it to updateCurrentHostState.", "dimension": "flows", "critical": true, "applicability": "required", "evidence_refs": ["source://hypervisor_state_manager.cpp#L91-L112"]},
    {"item_id": "state-branch-boot-progress-mapping", "statement": "SystemInitComplete maps to Standby, OSRunning maps to Running, Unspecified sets current and requested state to Off, and every other value sets current state to Off.", "dimension": "branches", "critical": true, "applicability": "required", "evidence_refs": ["source://hypervisor_state_manager.cpp#L60-L89"]},
    {"item_id": "state-current-host-state", "statement": "currentHostState logs only when the value changes, while updateCurrentHostState drives it to Standby, Running, or Off from BootProgress.", "dimension": "states", "critical": true, "applicability": "required", "evidence_refs": ["source://hypervisor_state_manager.cpp#L45-L89"]},
    {"item_id": "state-resource-message-payload", "statement": "The signal handler decodes a status-interface string and a string-valued property map, and performs no state update when BootProgress is absent.", "dimension": "resources", "critical": false, "applicability": "required", "evidence_refs": ["source://hypervisor_state_manager.cpp#L93-L110"]},
    {"item_id": "state-boundary-dbus-signal-property", "statement": "The D-Bus match is scoped to /xyz/openbmc_project/state/host0 and BootProgress::interface rather than accepting arbitrary property-change signals.", "dimension": "boundaries", "critical": true, "applicability": "required", "evidence_refs": ["source://hypervisor_state_manager.hpp#L47-L49"]},
    {"item_id": "state-concurrency-dbus-match-callback", "statement": "bootProgressChangeSignal owns a callback capturing this, so each matching D-Bus event is dispatched to the same Hypervisor instance.", "dimension": "concurrency", "critical": false, "applicability": "required", "evidence_refs": ["source://hypervisor_state_manager.hpp#L45-L50"]},
    {"item_id": "state-error-message-parse-return", "statement": "An sdbusplus exception while reading the signal is logged and returns before the BootProgress lookup and host-state update.", "dimension": "errors", "critical": true, "applicability": "required", "evidence_refs": ["source://hypervisor_state_manager.cpp#L91-L104"]},
    {"item_id": "state-mutation-boot-progress-host-state", "statement": "Unspecified is the only recognized boot-progress branch that mutates both CurrentHostState and RequestedHostTransition; the other branches mutate only current state.", "dimension": "mutation", "critical": true, "applicability": "required", "description": "SystemInitComplete sets the current host state to Standby, OSRunning sets Running, and Unspecified sets both current state and requested transition to Off.", "evidence_refs": ["source://hypervisor_state_manager.cpp#L64-L82"]}
  ]
}
